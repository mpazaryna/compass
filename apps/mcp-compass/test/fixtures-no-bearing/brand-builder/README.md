# Fixture: a bearing home nobody finished

Deliberately incomplete: this directory is a bearing home with support material
but **no `bearing.yaml`**. Used to prove the bake fails the build — naming the
home and the missing file — rather than silently serving nothing.
